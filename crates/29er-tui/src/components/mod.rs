//! App-owned native components vendored from the NMP registry
//! (`nmp add component …`). Refresh with `nmp update component <id>`; edits are
//! preserved and reported as conflicts on update.
pub mod nostr_chat;
pub mod nostr_content;
pub mod nostr_user;
